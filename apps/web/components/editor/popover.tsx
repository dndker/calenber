import {
    Popover,
    PopoverContent,
    PopoverTrigger,
} from "@workspace/ui/components/popover"

export const BNPopover = {
    Root: Popover,
    Trigger: PopoverTrigger,
    Content: PopoverContent,
}
